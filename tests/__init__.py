"""Test package for habit_tracker.

Tests are organised one-to-one with the source modules under
``habit_tracker/`` so it is always obvious where to find the tests
that cover a given piece of code.
"""
